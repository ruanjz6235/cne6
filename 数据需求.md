以下是 Barra CNE6 因子模型计算所需的源数据字段清单，按字段类别整理为表格：

### 基础标识列
| 字段名 | 说明 | 用途因子 |
|--------|------|----------|
| `secu_code` | 股票代码 | 所有因子（分组标识） |
| `end_date` | 交易日期（或报告期） | 所有因子（时间序列索引） |

### 行情与市场数据
| 字段名 | 说明 | 用途因子 |
|--------|------|----------|
| `pct_change` | 个股收益率（日/月） | Volatility（BETA/HSIGMA/DSTD/CMRA）、Momentum（RSTR/STREV）、Value（LTRSTR） |
| `index_return` | 市场指数收益率（基准收益） | Volatility（BETA/HSIGMA回归）、Momentum（RSTR超额收益）、Value（LTRSTR超额收益） |
| `close` | 收盘价 | Quality（PRED_VOEP分母） |
| `market_value` | 总市值 | Size（LNCAP/MIDCAP）、Value（EPTTM分母） |

### 财务数据（TTM与年末）
| 字段名 | 说明 | 用途因子 |
|--------|------|----------|
| `net_profit_ttm` | 净利润（TTM） | Value（EPTTM分子）、Quality（ROA分子、VOE） |
| `ebit_ttm` | 息税前利润（TTM） | Value（EBIT_EV分子） |
| `corp_val_crcp` | 企业价值（含现金及等价物） | Value（EBIT_EV分母） |
| `total_liability` | 总负债 | Quality（DTOA分子） |
| `total_asset` | 总资产 | Quality（DTOA分母、AT分母、GP分母、ROA分母、ABS分母、TAG回归对象） |
| `total_income_ttm` | 营业总收入（TTM） | Quality（AT分子、GP分子、GPM分母、VOS） |
| `total_cost_ttm` | 营业总成本（TTM） | Quality（GP分子减项、GPM分子减项） |
| `cash_flow_ttm` | 经营活动现金流净额（TTM） | Quality（VOC） |
| `accr_bs` | 资产负债表应计项目 | Quality（ABS分子，带负号） |
| `circulate_shares` | 流通股本 | Quality（IG回归对象） |
| `capex` | 资本支出 | Quality（CEG回归对象） |
| `eps` | 每股收益 | Growth（EGRO回归对象） |
| `income_ps` | 每股营业收入 | Growth（SGRO回归对象） |

### 分析师预测数据
| 字段名 | 说明 | 用途因子 |
|--------|------|----------|
| `pred_divd_rate` | 预测股息率 | DividendYield（DY均值计算） |
| `divd_rate_ttm` | 过去12个月股息率（TTM） | DividendYield（DY均值计算） |
| `pred_eps_std` | 分析师预测EPS标准差 | Quality（PRED_VOEP分子） |
| `pred_net_prof_gtrt` | 预测净利润增长率 | Growth（EGRLF直接映射） |

### 流动性指标
| 字段名 | 说明 | 用途因子 |
|--------|------|----------|
| `mth_tnrt` | 月换手率 | Liquidity（stom） |
| `quar_tnrt` | 季换手率 | Liquidity（stoq） |
| `ann_tnrt` | 年换手率 | Liquidity（stoa） |

### 中间计算结果（跨因子依赖）
| 字段名 | 说明 | 用途因子 |
|--------|------|----------|
| `alpha` 残差（来自 `VolatilityFactor.result.alpha`） | 个股相对市场的超额收益残差 | Momentum（HALPHA）、Value（LTHALPHA） |

> **补充说明**：
> - Quality 因子中计算 `VOS`、`VOE`、`VOC`、`TAG`、`IG`、`CEG` 需要 **连续5年历史数据**，因此相应财务字段（如 `total_income_ttm`、`net_profit_ttm`、`cash_flow_ttm`、`total_asset`、`circulate_shares`、`capex`）在时间维度上必须包含足够长的回溯窗口。
> - 若需实现完整 Barra 模型（含行业因子），还需额外提供 `industry_code` 或 `sector_code` 字段。